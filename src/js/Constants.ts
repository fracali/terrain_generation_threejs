export default class {
  // Renderer
  static globalSceneExposure = 1.2;
  static skyRadius = 4000;
  static cameraFov = 50;
  static cameraFar = 5000;
  static cameraNear = 0.2;
  static fogDensity = 0;

  // Terrain height
  static terrainHeightIntensity = 0.005;

  // Terrain size
  static terrainWidth = 10;
  static terrainDepth = 10;
  static terrainWidthRes = 150;
  static terrainDepthRes = 150;

  // Terrain noise
  static noiseSpeed = 0.4;
}
